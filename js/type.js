const typed = new Typed('#typed', {
  stringsElement: '#typed-strings',
  typeSpeed: 70,
  startDelay: 60,
  backSpeed: 50,
  backDelay: 500,
  loop: true,
  loopCount: false,
  showCursor: false,
});